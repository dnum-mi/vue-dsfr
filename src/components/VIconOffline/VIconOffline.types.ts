/** Collections Iconify locales fournies par l’application. */
export type VIconOfflineProps = {
  /** Nom complet de l’icône, au format `prefix:nom`. */
  name: string
  /** Libellé accessible de l’icône. */
  label?: string
  /** Titre de l’icône, utilisé comme nom accessible si `label` est absent. */
  title?: string
}
